apiVersion: apps/v1
kind: Deployment
metadata:
  name: sri-app
  namespace: sri-qa 
spec:
  replicas: 2 
  selector:
    matchLabels:
      app: sri-app
  template:
    metadata:
      labels:
        app: sri-app
    spec:
      containers:
      - name: sri-app
        # Make sure this matches your actual ECR registry
        image: 812698982757.dkr.ecr.ap-south-1.amazonaws.com/sri-ecr-repo:latest
        ports:
        - containerPort: 80
        resources:
          requests:
            cpu: 100m
            memory: 128Mi
          limits:
            cpu: 500m
            memory: 512Mi
        livenessProbe:
          httpGet:
            path: /health
            port: 80
          initialDelaySeconds: 30
        readinessProbe:
          httpGet:
            path: /health
            port: 80
          initialDelaySeconds: 10
