apiVersion: v1
kind: Service
metadata:
  name: sri-app-service
  namespace: sri-qa
spec:
  type: ClusterIP
  selector:
    app: sri-app  # Fixed: Indented 2 spaces
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80 # Matches your Docker Expose 80
